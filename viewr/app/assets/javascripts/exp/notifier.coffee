window.viewr.Notifier = class
  
  constructor: (@settings) ->

  notify: (payload) ->
    # Calls the other 3 methods based on settings
  
  soundNotify: ->

  popupNotify: (payload) ->

  titleNotify: ->
