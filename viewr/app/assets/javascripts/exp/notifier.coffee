class window.viewr.Notifier
  
  constructor: (@settings) ->

  notify: (payload) ->
    # Calls the other 3 methods based on settings
  
  soundNotify: ->

  popupNotify: (payload) ->

  titleNotify: ->
